type Props = {
  params: {
    resumeId: string
  }
}

export default async function BuilderPreviewPage(props: Props) {
  return (
    <div className="w-full h-screen flex justify-center items-center">
    </div>
  )
}